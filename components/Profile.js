import React from 'react';
import {
    View,
    Text,
    StyleSheet,
    Image,
    ScrollView,
    ImageBackground,
} from 'react-native';

const Profile = ({}) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};

export default Profile;